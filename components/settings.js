import { Text, View } from "react-native";

const settings = () => {
    return (
        <View style = {{flex: 1, alignItems: 'center', backgroundColor: '#000'}}>
            <Text style = {{color: '#fff', fontSize:18}}>Settings Page</Text>
        </View>
    ) 
}

export default settings;